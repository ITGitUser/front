import Layout from '../../common/Layout/Layout.jsx'

const Timer = () =>{

    
    

    return (
        <Layout>
            <h1 style={{color: 'gray', width: "100%", textAlign: "center" }}>Затраченное время</h1>
        </Layout>
    );
};

export default Timer;